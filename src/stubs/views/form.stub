@extends('layout')
@section('content')

    <x-page-card>
        <h2 class="card-title mt-2">
            {title}
        </h2>
        <x-form action="{{route('{submitRoute}' {updateParameter})}}" method="{method}">
            {translationSelector}
            <div class="row">
                {components}
            </div>
        </x-form>
    </x-page-card>

@endsection
